version: '3.7'

services:

  php:
    build:
      context: ./src
      dockerfile: ../docker/php/Dockerfile
      args:
        PHP_VERSION: ${PHP_VERSION}
    container_name: ${PROJECT_NAME}-application
    env_file:
      - .env
    volumes:
      - ./src:/var/www/html
    ports:
      - "9000:9000"
    depends_on:
      - mysql
      - postgres
    links:
      - mysql
      - postgres
    networks:
      - symfony

  nginx:
    image: nginx:stable-alpine
    container_name: ${PROJECT_NAME}-nginx
    ports:
      - "${WEBSERVER_PORT}:80"
    volumes:
      - ./src:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
      - mysql
      - postgres
    networks:
      - symfony

  mysql:
    image: mysql:${MYSQL_VERSION}
    container_name: ${PROJECT_NAME}-mysql
    ports:
      - "${MYSQL_PORT}:3306"
    env_file:
      - .env
    command: --default-authentication-plugin=mysql_native_password
    volumes:
      - symfony-mysql-data:/var/lib/mysql
    networks:
      - symfony

  postgres:
    image: postgres:${POSTGRES_VERSION}-alpine
    container_name: ${PROJECT_NAME}-postgresql
    env_file:
      - .env
    volumes:
      - symfony-postgres-data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT}:5432"
    networks:
      - symfony

networks:
  symfony:

volumes:
  symfony-mysql-data:
  symfony-postgres-data:
